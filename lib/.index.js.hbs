/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 */

'use strict'

const create = require('./create')
const AKV = require('./akv')

let lib = create.bind(this)

Object.assign(lib, AKV, {
  AKV,
  create
})

module.exports = lib
