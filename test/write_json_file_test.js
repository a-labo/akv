/**
 * Test case for writeJsonFile.
 * Runs with mocha.
 */
'use strict'

const writeJsonFile = require('../lib/file/write_json_file.js')
const assert = require('assert')
const co = require('co')

describe('write-json-file', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Write json file', () => co(function * () {

  }))
})

/* global describe, before, after, it */
