/**
 * Test case for readJsonFile.
 * Runs with mocha.
 */
'use strict'

const readJsonFile = require('../lib/file/read_json_file.js')
const assert = require('assert')
const co = require('co')

describe('read-json-file', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Read json file', () => co(function * () {

  }))
})

/* global describe, before, after, it */
