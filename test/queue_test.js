/**
 * Test case for queue.
 * Runs with mocha.
 */
'use strict'

const queue = require('../lib/queue.js')
const assert = require('assert')
const co = require('co')

describe('queue', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Queue', () => co(function * () {

  }))
})

/* global describe, before, after, it */
